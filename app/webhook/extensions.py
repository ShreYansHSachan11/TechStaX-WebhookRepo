"""
Flask Extensions Configuration

This module initializes and configures Flask extensions used by the application.
Currently, the application uses minimal extensions to keep it lightweight.
"""

# Future extensions can be initialized here
# For example:
# from flask_cors import CORS
# cors = CORS()

# Currently no extensions are needed as CORS is handled manually
# and the application uses minimal dependencies